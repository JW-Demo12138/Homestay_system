## 删除首页地图及相关功能的计划

### 1. 删除模板中的地图展示部分
- 移除 HomeView.vue 文件中第 63-71 行的 GIS 地图展示部分
- 具体删除 `<section class="map-section">` 标签及其内部内容

### 2. 删除脚本中的组件导入
- 移除 HomeView.vue 文件中第 107 行的 HomestayMap 组件导入
- 具体删除 `import HomestayMap from '../components/HomestayMap.vue'` 语句

### 3. 删除相关样式
- 移除 HomeView.vue 文件中第 302-315 行的 GIS 地图部分样式
- 具体删除 `.map-section` 和 `.map-wrapper` 相关的样式规则

### 4. 验证修改
- 运行前端构建命令，确保代码可以正常编译
- 确认修改只影响首页，不影响其他组件中地图的使用

### 注意事项
- 这些修改只会影响首页（HomeView.vue），不会修改或删除 HomestayMap 组件本身
- 其他组件（如 LandlordMapSelector.vue 等）中使用的地图功能将保持不变
- 确保删除后的代码语法正确，布局合理